module.exports = {
  name: 'hacked',
  description: 'Kick hacked discord account that not being detected by void channel!',
  execute(message, args, client) {
    return
    let memberID = args[0]
    let messageID = args[1]
  }
}