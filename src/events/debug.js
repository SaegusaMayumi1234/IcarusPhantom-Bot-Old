module.exports = {
	name: 'debug',
	execute(info, client) {
    //onsole.log(info)
    //if (info.includes("Session Limit Information"))
		/*setTimeout(() => {
      let channel = client.channels.cache.get('890422767869104178');
      channel.send({
        embed: {
          author: { name: `Login Limit Info` },
          description: info,
          color: '47F049'
        }
      })
    }, 15000)*/
	},
};